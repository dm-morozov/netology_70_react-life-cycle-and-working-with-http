# 🎓 Жизненный цикл и работа с HTTP в React

Этот проект является практическим заданием по курсу React от Netology. Он фокусируется на работе с жизненным циклом компонентов (используя хуки вроде `useEffect`), асинхронными операциями, таймерами, очисткой эффектов и взаимодействием с HTTP (CRUD-операции). Проект написан на React с TypeScript, без использования классовых компонентов — только функциональные с хуками.

## Статус проекта

[![React + TypeScript Build](https://github.com/dm-morozov/netology_70_react-life-cycle-and-working-with-http/actions/workflows/web.yaml/badge.svg)](https://github.com/dm-morozov/netology_70_react-life-cycle-and-working-with-http/actions/workflows/web.yaml)

![Netology](https://img.shields.io/badge/React-TypeScript-blue)

---

[Посетить сайт проекта!](https://dm-morozov.github.io/netology_70_react-life-cycle-and-working-with-http/)

---

## 📝 Описание проекта

Проект демонстрирует ключевые концепции React:

- **Жизненный цикл компонентов**: Монтирование, обновление и размонтирование с помощью `useEffect`.
- **Работа с асинхронными данными**: Загрузка данных с API, таймеры, интервалы и их очистка для предотвращения утечек памяти.
- **HTTP-взаимодействие**: CRUD-операции (Create, Read, Update, Delete) с использованием `fetch`.
- **Оптимизации**: Использование `useCallback` и `useMemo` для предотвращения ненужных рендеров и вычислений.
- **UI-компоненты**: Простые формы, списки, аналоговые часы с анимацией стрелок.

Проект включает классовые упражнения (простые компоненты для демонстрации хуков) и домашние задания (более сложные приложения: "Мировые часы" и "CRUD для заметок").

Backend для CRUD-задания выложен на Render (https://netology-backend.onrender.com) для демонстрации реальной работы с удаленным сервером. Локальный backend также доступен в папке `Backend` (на Node.js).

## 🚀 Что мы сделали

### Классовые упражнения (демонстрация `useEffect`)

Эти компоненты созданы для практики базовых сценариев жизненного цикла:

1. **RandomFact**:

   - Загружает случайный факт о кошках с API (https://catfact.ninja/fact) при монтировании.
   - Обновляет факт каждые 10 секунд с помощью `setInterval`.
   - Очищает интервал при размонтировании (аналог `componentWillUnmount`).
   - Использует `useState` для хранения факта и `useEffect` с пустыми зависимостями для запуска только при монтировании.

2. **TimerWithCleaning**:

   - Таймер, увеличивающий счетчик секунд каждую секунду (`setInterval`).
   - Очищает интервал при размонтировании, чтобы избежать утечек (логирует "Таймер остановлен").
   - Демонстрирует cleanup-функцию в `useEffect` (аналог `componentWillUnmount`).

3. **UserList**:
   - Загружает список пользователей с API (https://jsonplaceholder.typicode.com/users) при монтировании.
   - Обрабатывает состояния загрузки, ошибки и отображает список имен.
   - Использует `fetch` с цепочкой `then/catch/finally` для асинхронной логики.

### Домашние задания

1. **Мировые часы (WorldClocks)**:

   - Приложение для добавления и отображения часов в разных часовых поясах.
   - **Функционал**:
     - Форма для ввода названия города и смещения от UTC (в часах).
     - Добавление часов: создает новый компонент `ItemClock` с аналоговыми стрелками.
     - Удаление часов: по клику на крестик, с очисткой интервала.
     - Каждые часы тикают independently (обновление времени каждую секунду с `setInterval`).
   - **Технические детали**:
     - `useEffect` для запуска интервала и расчета времени с учетом смещения (использует `Date` и `getTimezoneOffset`).
     - `useMemo` для оптимизации расчета углов стрелок (часовая, минутная, секундная) — предотвращает лишние вычисления.
     - `useCallback` для функции удаления, чтобы избежать пересоздания при рендерах.
     - CSS-анимация стрелок с `transform: rotate()`.
   - **Упрощения**: Аналоговые часы реализованы полностью (не цифровые). Поддержка Moment.js не использовалась — только нативный `Date`.

2. **CRUD для заметок (NotesApp)**:
   - Приложение для управления заметками с полным CRUD (без Update, как указано в задании).
   - **Функционал**:
     - **Read**: Загрузка списка заметок с сервера при монтировании и по кнопке "Обновить" (GET /notes).
     - **Create**: Добавление новой заметки через форму (POST /notes с JSON {id: 0, content: "..."}).
     - **Delete**: Удаление по ID (DELETE /notes/{id}).
     - Обработка состояний: загрузка, ошибки, пустой список.
     - После каждой операции (add/delete) — автоматическое обновление списка.
   - **Технические детали**:
     - `useEffect` для первоначальной загрузки.
     - `useCallback` для функции fetchNotes (чтобы избежать лишних зависимостей).
     - Асинхронные функции с `async/await` и обработкой ошибок.
     - API_URL: Локально — http://localhost:7070/notes; на Render — https://netology-backend.onrender.com/notes.
   - **Backend**:
     - Локальный: Node.js сервер в папке `Backend` (запускается с `node index.js` на порту 7070).
     - Удаленный: Выложен на Render для production-тестирования (бесплатный хостинг, может быть задержки при холодном старте).

## 🧠 Чему мы научились (подробно)

Этот проект помог углубить понимание React-хаков и лучших практик:

- **useEffect и жизненный цикл**:

  - Монтирование: Запуск кода только при первом рендере (пустые зависимости `[]` — аналог `componentDidMount`).
  - Обновление: Реакция на изменения пропсов/состояния (зависимости в массиве — аналог `componentDidUpdate`).
  - Размонтирование: Cleanup-функция для очистки таймеров, интервалов, подписок (аналог `componentWillUnmount`). Это критично для предотвращения утечек памяти — например, в таймерах и API-запросах.
  - Комбинации: В RandomFact — комбинация монтирования, обновления по интервалу и очистки.

- **Асинхронные операции**:

  - Использование `fetch` для HTTP-запросов (GET, POST, DELETE).
  - Обработка промисов с `then/catch/finally` или `async/await`.
  - Состояния загрузки/ошибок: `useState` для isLoading, error — улучшает UX (показ спиннера или сообщений).

- **Оптимизации**:

  - `useCallback`: Мемоизация функций (например, handleRemoveClock), чтобы они не пересоздавались при рендерах и не вызывали лишние эффекты в дочерних компонентах.
  - `useMemo`: Кэширование вычислений (углы стрелок в ItemClock) — экономит ресурсы, особенно в циклах обновлений.

- **Работа с формами и UI**:

  - Контролируемые инпуты: `value` и `onChange` для состояний.
  - Динамические списки: `map` для рендера компонентов с уникальными `key`.
  - CSS-модули: Для стилизации (например, WorldСlocks.module.css) — изоляция стилей.

- **Общие уроки**:
  - Важность очистки эффектов: Без cleanup таймеры продолжают работать даже после удаления компонента, вызывая ошибки или утечки.
  - Обработка ошибок: Try/catch в асинхронном коде, пользовательские сообщения.
  - TypeScript: Строгая типизация пропсов, состояний (интерфейсы как UserListProps, ClockItem).
  - Деплой: GitHub Pages для фронтенда, Render для бэкенда — практика реального хостинга.

Проект помог перейти от теории к практике, понять, как React управляет обновлениями и почему хуки — мощный инструмент для функциональных компонентов.

## 🗂 Структура проекта

```
.
├── Backend/              # Локальный Node.js backend для CRUD
├── node_modules/         # Зависимости
├── public/               # Публичные файлы (index.html и т.д.)
├── src/                  # Исходный код
│   ├── assets/           # Изображения (например, watches.png)
│   ├── components/       # Компоненты
│   │   ├── classwork_with_GPT/  # Классовые упражнения
│   │   │   ├── RandomFact/      # Компонент случайного факта
│   │   │   ├── TimerWithCleaning/ # Таймер с очисткой
│   │   │   └── UserList/        # Список пользователей
│   │   ├── Notes/               # CRUD-приложение
│   │   │   └── NotesApp.tsx     # Основной компонент
│   │   └── WorldСlocks/         # Мировые часы
│   │       ├── ItemClock.tsx    # Отдельные часы
│   │       └── WorldСlocks.tsx  # Основной компонент
│   ├── types/            # TypeScript-интерфейсы (Note, ClockItem и т.д.)
│   ├── App.tsx           # Корневой компонент (рендерит все)
│   ├── index.css         # Глобальные стили
│   └── main.tsx          # Входная точка
├── svg/                  # Иконки (для контактов)
├── .github/              # GitHub Actions (CI/CD)
├── .gitignore            # Игнорируемые файлы
├── descriptionNetology.md # Описание заданий от Netology
├── eslint.config.js      # ESLint конфиг
├── package.json          # Зависимости и скрипты
├── README.md             # Этот файл
├── tsconfig.json         # TypeScript конфиг
├── vite.config.ts        # Vite конфиг (для сборки)
└── yarn.lock             # Лок зависимости
```

## 🛠 Установка и запуск

1. Клонируйте репозиторий: `git clone https://github.com/dm-morozov/netology_70_react-life-cycle-and-working-with-http.git`
2. Установите зависимости: `yarn install` (или `npm install`)
3. Запустите локально: `yarn dev` (или `npm run dev`) — откроется на http://localhost:5173
4. Для backend:
   - Локально: `cd Backend && node index.js` (порт 7070)
   - Удаленно: Используйте https://netology-backend.onrender.com в коде (обновите API_URL в NotesApp.tsx)
5. Сборка: `yarn build` (для production)

Зависимости: React, TypeScript, Vite. Нет внешних библиотек вроде Moment.js — все нативно.

## 📧 Контакты

Если возникнут вопросы, пишите:

- ![LinkedIn](./svg/linkedin-icon.svg) [LinkedIn](https://www.linkedin.com/in/dm-morozov/)
- ![Telegram](./svg/telegram.svg) [Telegram](https://t.me/dem2014)
- ![GitHub](./svg/github-icon.svg) [GitHub](https://github.com/dm-morozov/)
